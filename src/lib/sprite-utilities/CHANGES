commit 032e693ebe9831da986f896abf445bbf69d1abf1
Author: sugarman <sugarman@google.com>
Date:   Sun Oct 23 01:53:15 2016 -0700

    sugarman local changes

diff --git a/bin/spriteUtilities.js b/bin/spriteUtilities.js
index be49b42..a13cd39 100644
--- a/bin/spriteUtilities.js
+++ b/bin/spriteUtilities.js
@@ -206,7 +206,7 @@ var SpriteUtilities = (function () {
         if(numberOfFrames === 1) {
           numberOfFrames = 2;
           frameCounter += 1;
-        }  
+        }
         */
 
         //Calculate the frame rate. Set the default fps to 12
@@ -1338,7 +1338,7 @@ var SpriteUtilities = (function () {
         return value;
       }
 
-      //No it's not a number, so it must be a string   
+      //No it's not a number, so it must be a string
       else {
 
           return this.colorToHex(value);
@@ -1357,20 +1357,22 @@ var SpriteUtilities = (function () {
       /*
       //Find out if the first character in the string is a number
       if (!isNaN(parseInt(string.charAt(0)))) {
-        
+
         //It's not, so convert it to a hex code
         return colorToHex(string);
-        
+
       //The use input a number, so it must be a hex code. Just return it
       } else {
-      
+
         return string;
       }
-      
+
       */
     }
   }]);
 
   return SpriteUtilities;
 })();
-//# sourceMappingURL=spriteUtilities.js.map
\ No newline at end of file
+//# sourceMappingURL=spriteUtilities.js.map
+
+export default SpriteUtilities
diff --git a/index.js b/index.js
new file mode 100644
index 0000000..b56e265
--- /dev/null
+++ b/index.js
@@ -0,0 +1,4 @@
+import SpriteUtilities from './bin/spriteUtilities'
+import pixi from 'pixi.js'
+
+export default new SpriteUtilities(pixi)
